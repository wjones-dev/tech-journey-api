spring:
    application:
        name: tech-journey-api

    datasource:
        url: jdbc:h2:mem:techjourney
        driver-class-name: org.h2.Driver
        username: sa
        password:

    jpa:
        hibernate:
            ddl-auto: create
        show-sql: true

    h2:
        console:
            enabled: true
